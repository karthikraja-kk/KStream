#!/usr/bin/env sh

# ... standard gradlew script content ...
# Since I can't provide the binary JAR, I'll just provide a skeleton 
# that might be replaced or fixed by the user if they have a local environment.
# Or I can try to find a minimal version.

exec ./gradle/wrapper/gradle-wrapper.jar "$@"
