<?xml version="1.0" encoding="utf-8"?>
<!--
  Focus indicator — clean accent-coloured border only. No inner fill,
  no glow halo.

  Border is drawn flush with the tile edge (no inset). With a 6dp stroke
  centered on the edge, the outer 3dp is clipped by Leanback's row
  view and the inner 3dp remains visible as a uniform border that
  visually hugs the tile on all four sides + corners.

  Corner radius matches the poster background (card_poster_bg uses
  card_poster_corner = 16dp) so the ring's curves align with the
  rounded artwork edge.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="6dp"
                android:color="@color/accent_primary" />
            <corners android:radius="16dp" />
        </shape>
    </item>
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="2dp"
                android:color="@color/accent_primary" />
            <corners android:radius="16dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>
</selector>
