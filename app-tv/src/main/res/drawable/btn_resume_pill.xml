<?xml version="1.0" encoding="utf-8"?>
<!--
  Resume pill button background. Rounded magenta with focused / pressed states.
  Used for the unified Resume + chevron button on details. A 2dp progress
  hairline is drawn separately as a child View clipped by the pill's rounded
  corners (clipToOutline).
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/accent_gold" />
            <corners android:radius="10dp" />
            <stroke android:width="2dp" android:color="@color/text_primary" />
        </shape>
    </item>
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/accent_gold_muted" />
            <corners android:radius="10dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/accent_gold" />
            <corners android:radius="10dp" />
        </shape>
    </item>
</selector>
